// IGNORE THIS FILE ITS ONLY USED TO VIEW THE APP THROUGH SNACK EXPO
import { registerRootComponent } from "expo";
import App from "./App";

export default function SnackCompatibility() {
  return <App />;
}

registerRootComponent(SnackCompatibility);
